name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  runtime:
    name: Agent runtime (packages/agent-runtime)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - run: cd packages/agent-runtime && npm install
      - run: cd packages/agent-runtime && npm run typecheck
      - run: cd packages/agent-runtime && npm test

  agents-and-examples:
    name: Agents + examples (typecheck + tests)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Build the runtime (dependents need its dist)
        run: cd packages/agent-runtime && npm install && npm run build
      - name: marketplace feed (typecheck + tests)
        run: cd examples/marketplace/feed && npm install && npm run typecheck && npm test
      - name: marketplace web (typecheck)
        run: cd examples/marketplace/web && npm install && npm run typecheck
      - name: txodds (typecheck)
        run: cd examples/txodds && npm install && npm run typecheck
